# For running the backend directly on your host (outside Docker), against
# the Postgres started by `docker compose up db` or a local install.
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/helpdesk
API_PORT=8000
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
